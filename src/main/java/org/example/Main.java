package org.example;

public class Main { // class name should always start with an uppercase first letter
    // class names are case-sensitive
    // name of class MUST be the same as the class name
    public static void main(String[] args) {
        String name = "John";
        System.out.println("Hello " + name);
    }

    static void userBank() {
        final int userID;
        int userBalance;
    }

    int userID;
    int userBalance;

    static void withdraw(int userID, int amount) {

    }

    static void deposit(int userID, int amount) {}

    static float checkBalance(int userID) {
        return 0f;
    }
}
